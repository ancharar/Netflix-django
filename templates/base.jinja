<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ page_title or "Netflix DB" }}</title>

  <style>
    :root{
      --bg:#0b0f19;
      --card:#121a2a;
      --card2:#0f1626;
      --text:#e7eefc;
      --muted:#a8b6d6;
      --border:rgba(255,255,255,.12);
      --accent:#e50914; /* netflix red */
      --accent2:#6ea8fe;
    }
    *{ box-sizing:border-box }
    body{
      margin:0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      color:var(--text);
      background:
        radial-gradient(900px 450px at 10% 0%, rgba(229,9,20,.25), transparent 60%),
        radial-gradient(900px 450px at 90% 10%, rgba(110,168,254,.18), transparent 55%),
        var(--bg);
    }
    a{ color:inherit; text-decoration:none }
    .container{ max-width:1200px; margin:0 auto; padding:22px 16px 60px; }

    .topbar{
      display:flex; align-items:center; justify-content:space-between; gap:12px;
      padding:14px 16px;
      border:1px solid var(--border);
      border-radius:16px;
      background: rgba(18,26,42,.78);
      backdrop-filter: blur(8px);
      box-shadow: 0 12px 34px rgba(0,0,0,.35);
    }
    .brand{
      display:flex; align-items:center; gap:12px;
    }
    .logo{
      width:12px; height:28px; border-radius:6px;
      background: linear-gradient(180deg, var(--accent), #ff4d4d);
      box-shadow: 0 0 18px rgba(229,9,20,.45);
    }
    .title{
      margin:0; font-size:18px; letter-spacing:.2px;
    }
    .subtitle{ margin:2px 0 0; font-size:12px; color:var(--muted); }

    .actions{ display:flex; gap:10px; flex-wrap:wrap; }
    .btn{
      padding:8px 12px; border-radius:999px;
      border:1px solid var(--border);
      background: rgba(15,22,38,.55);
      color:var(--text);
      font-size:13px;
    }
    .btn:hover{ border-color: rgba(255,255,255,.22); }
    .btn.primary{
      border-color: rgba(229,9,20,.55);
      background: rgba(229,9,20,.14);
    }

    .grid{
      display:grid;
      grid-template-columns: 1fr;
      gap:14px;
      margin-top:14px;
    }
    @media (min-width: 980px){
      .grid{ grid-template-columns: 1fr 1fr; }
    }

    .card{
      border:1px solid var(--border);
      border-radius:16px;
      background: rgba(18,26,42,.78);
      box-shadow: 0 12px 34px rgba(0,0,0,.28);
      overflow:hidden;
    }
    .card-header{
      padding:12px 14px;
      display:flex; align-items:baseline; justify-content:space-between; gap:10px;
      background: linear-gradient(180deg, rgba(255,255,255,.06), transparent);
      border-bottom:1px solid var(--border);
    }
    .card-title{ margin:0; font-size:14px; letter-spacing:.2px; }
    .chip{
      font-size:12px; color:var(--muted);
      padding:3px 10px; border-radius:999px;
      border:1px solid var(--border);
      background: rgba(15,22,38,.5);
      white-space:nowrap;
    }
    .card-body{ padding:0; }

    .table-wrap{ overflow:auto; }
    table{
      width:100%;
      border-collapse: collapse;
      min-width: 560px;
      background: rgba(0,0,0,.08);
    }
    th, td{
      padding:10px 12px;
      border-bottom:1px solid var(--border);
      vertical-align: top;
      white-space: nowrap;
    }
    th{
      position: sticky;
      top: 0;
      z-index: 1;
      background: rgba(18,26,42,.95);
      text-align:left;
      font-weight: 650;
      font-size: 12.5px;
      letter-spacing: .2px;
    }
    td{ font-size: 13px; color: var(--text); }
    tr:hover td{ background: rgba(110,168,254,.08); }

    .muted{ color:var(--muted); }
    .footer{
      margin-top:16px;
      text-align:center;
      color:var(--muted);
      font-size:12px;
    }
    .section-title{
      margin:18px 2px 8px;
      color:var(--muted);
      font-size:12px;
      letter-spacing:.25em;
      text-transform:uppercase;
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="topbar">
      <div class="brand">
        <div class="logo"></div>
        <div>
          <h1 class="title">{{ page_title or "Netflix Database" }}</h1>
          <div class="subtitle">{{ page_subtitle or "Django + PostgreSQL + Jinja2 (таблицы и статистика)" }}</div>
        </div>
      </div>

      <div class="actions">
        <a class="btn primary" href="/">Главная</a>
        <a class="btn" href="/admin/">Admin</a>
      </div>
    </div>

    {% block content %}{% endblock %}

    <div class="footer">
      Нормализованная БД (3НФ). Таблицы строятся макросами Jinja2.
    </div>
  </div>
</body>
</html>
